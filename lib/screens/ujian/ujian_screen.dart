import 'package:flutter/material.dart';

class UjianScreen extends StatelessWidget {
  const UjianScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ujian Screen"),
      ),
    );
  }
}